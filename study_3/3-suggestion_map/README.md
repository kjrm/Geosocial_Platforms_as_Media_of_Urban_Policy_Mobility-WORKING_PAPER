# suggestion map

The repository includes the code (`study3-3.ipynb`) and data (`labelled_reviews.csv`) necessary to complete the third and final part of the study.

The code also requires the shapefile `` which is included in the `1-sentiment_flows` repository.
